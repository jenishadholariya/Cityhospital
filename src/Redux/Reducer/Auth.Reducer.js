

const initval={
    isloading : false,
    error : '',
    user : null
}

export const AuthReducer = (state = initval, action) =>{
    switch(action.type){
        default :
            return state
    }
}